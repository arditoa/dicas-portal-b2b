{
  "buildCommand": "cd portal-cadastro && npm install && npm run build",
  "outputDirectory": "portal-cadastro/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
